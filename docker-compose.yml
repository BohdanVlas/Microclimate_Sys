version: "3.8"
services:
  simulator:
    build: .
    container_name: microclimate_simulator
    command: ["python", "microclimate_sim.py", "--logfile", "/microclimate_log.csv", "--run-seconds", "0"]
    restart: unless-stopped
